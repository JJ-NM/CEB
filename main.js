function myFunction() {
  // get the clock
  var myClock = document.getElementById("menu");

  // get the current value of the clock's display property
  var displaySetting = myClock.style.display;

  // also get the clock button, so we can change what it says
  var clockButton = document.getElementById("menuButton");

  // now toggle the clock and the button text, depending on current state
  if (displaySetting == "block") {
    // clock is visible. hide it
    myClock.style.display = "none";
    // change button text
  } else {
    // clock is hidden. show it
    myClock.style.display = "block";
    // change button text
  }
}

function myFunctionNov1() {
  // get the clock
  var myClock = document.getElementById("nov1-text");

  // get the current value of the clock's display property
  var displaySetting = myClock.style.display;

  // also get the clock button, so we can change what it says
  var clockButton = document.getElementById("menuButton");

  // now toggle the clock and the button text, depending on current state
  if (displaySetting == "block") {
    // clock is visible. hide it
    myClock.style.display = "none";
    // change button text
  } else {
    // clock is hidden. show it
    myClock.style.display = "block";
    // change button text
  }
}

function myFunctionNov2() {
  // get the clock
  var myClock = document.getElementById("nov2-text");

  // get the current value of the clock's display property
  var displaySetting = myClock.style.display;

  // also get the clock button, so we can change what it says
  var clockButton = document.getElementById("menuButton");

  // now toggle the clock and the button text, depending on current state
  if (displaySetting == "block") {
    // clock is visible. hide it
    myClock.style.display = "none";
    // change button text
  } else {
    // clock is hidden. show it
    myClock.style.display = "block";
    // change button text
  }
}

function myFunctionNov3() {
  // get the clock
  var myClock = document.getElementById("nov3-text");

  // get the current value of the clock's display property
  var displaySetting = myClock.style.display;

  // also get the clock button, so we can change what it says
  var clockButton = document.getElementById("menuButton");

  // now toggle the clock and the button text, depending on current state
  if (displaySetting == "block") {
    // clock is visible. hide it
    myClock.style.display = "none";
    // change button text
  } else {
    // clock is hidden. show it
    myClock.style.display = "block";
    // change button text
  }
}
